<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happy.model.repo.RegionRepo">

    <insert id="insert" parameterType="User">
        insert into favorite_region (id, sidoName, gugunName, dongName)
        values (#{id}, #{sido}, #{gugun}, #{dong})
    </insert>

    <select id="selectAllFavoriteRegions" parameterType="User" resultType="Region">
        select sidoName, gugunName, dongName
        from favorite_region
        where id = #{id}
    </select>

</mapper>
