<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happy.model.repo.DealsRepo">

    <select id="selectAptName" resultType="Deals">
        SELECT count(*) as dealNum, aptCode, aptName, dongName, dongCode, lng, lat, avg(dealAmount) avgAmount
        FROM dealInfo
        GROUP BY AptCode
        HAVING dongCode = #{dongCode}
        ORDER BY ${sortBy} ${sortOrder}
    </select>

</mapper>